<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FinalRedEnvelopeActivity"
    android:background="@drawable/user_bg">

    <EditText
        android:id="@+id/et_envelope_money"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_above="@+id/btn_confirm_send"
        android:background="@drawable/input_design"
        android:gravity="center"
        android:layout_marginHorizontal="20dp"
        android:hint="請輸入要給的紅包錢"
        android:inputType="number"
        android:textSize="30dp"/>

    <Button
        android:id="@+id/btn_confirm_send"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_centerInParent="true"
        android:layout_marginTop="50dp"
        android:background="@drawable/final_btn_circle"
        android:gravity="center"
        android:layout_marginHorizontal="20dp"
        android:text="確定送出"
        android:textSize="25dp"
        android:textColor="@android:color/white" />
</RelativeLayout>